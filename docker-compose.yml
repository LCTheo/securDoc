version: '3'
services:
  rsc:
    build: rsc
    container_name: rsc
    ports:
      - "5000:5000"
  jwt:
    build: jwt
    container_name: jwt
  usr:
      build: usr
      container_name: usr
      ports:
        - "5001:5000"
  bd:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27016:27017"